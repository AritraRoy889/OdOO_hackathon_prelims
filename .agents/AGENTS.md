# Agent Rules

1. **Mandatory Git Commits:** Every time you finish making changes for a task, you MUST immediately stage and commit those changes using `git add .` and `git commit` with an appropriate message. Do not wait for the user to prompt you.
